import React from 'react';
import { View, Text, TouchableOpacity } from 'react-native';

const TodoCard = (props) => {
    return (
        <View>
            <Text>TodoCard</Text>
        </View>
    )
}

export { TodoCard }