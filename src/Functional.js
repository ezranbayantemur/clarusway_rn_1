import React, { useState } from 'react';
import { SafeAreaView, View, Text, Button } from 'react-native';

const App = (props) => {
    return (
        <SafeAreaView>
            <View>
                <Text style={{ fontSize: 80 }}>Hello 🌍 (Functional)</Text>
            </View>
        </SafeAreaView>
    )
}

export default App;